# 验收记录

验收日期：2026-09-22。环境：Windows、真实 Google Chrome / Playwright，桌面 1440×1000、窄屏 390×844。全部测试在隔离浏览器上下文内运行。结果是实际执行结果，不以生产构建代替交互验收。

证据：`tests/*.spec.js` 保存实际操作与断言；[E2E 结果](test-results/e2e.txt)、[单元结果](test-results/unit.txt)、[生产构建](test-results/build.txt)、[浏览器控制台](test-results/browser-console.json)。截图位于 `docs/screenshots/`；下表数字对应 [完整文件索引](screenshots/README.md)。

| 编号 | 需求与操作步骤 | 预期结果 | 实际结果 | 测试/日志证据 | 截图 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| F01 | 空标题、空格提交；只填标题；双击创建；选择字段 | 无效阻止，有效创建一次，默认待办/中 | 校验中文显示，空描述合法，双击只有1项 | model.test.js；round2；final | 02、03、04 | 已验证 |
| F02 | 创建后切换列表/看板，打开详情；空数据操作 | 两视图共享任务，完整详情可读 | ID/数量一致，多行详情完整，空态可创建 | round2、round4、final | 01、05、11、17 | 已验证 |
| F03 | 编辑回填，改描述/状态/优先级后保存、刷新 | 字段更新且 ID 不变 | 保存即时更新，刷新保持 | model.test.js；round2、round3、final | 06、07、16 | 已验证 |
| F04 | 打开具名确认；取消再删除；刷新 | 取消保留，确认只删目标且永久消失 | 目标删除，其余三项保留；全部删除后仍为空 | round2、round3、final | 08、09 | 已验证 |
| F05 | 分别创建三种状态；编辑和拖动修改状态 | 待办/进行中/完成稳定，其他字段不丢失 | 三状态均可创建展示，双向移动，字段/ID保留 | model.test.js；round2、round3、round4、final | 01、11、12 | 已验证 |
| F06 | 同屏显示三档，编辑中→高，刷新 | 高红、中黄、低绿；文字清晰且持久化 | 浅深主题对应正确，刷新仍高 | round2、final | 10、13、16 | 已验证 |
| F07 | 鼠标待办→进行中→完成→待办；同列、取消和空列 | 状态准确、总数不变、刷新位置恢复 | 真正鼠标按下/移动/松开成功，总数2保持，ID/内容不变 | round4.spec.js；e2e.txt | 11、12 | 已验证 |
| F08 | 浅→深、打开表单、刷新、深→浅、重新打开页面 | 全局适配并记忆选择 | 主题类和存储一致，深色恢复成功，表单可读 | final.spec.js；browser-console.json | 13、14、18 | 已验证 |
| F09 | 新增/编辑/删除/拖放后分别刷新，关页再开 | 同源持久化，空数组有效，异常不白屏 | 逐项恢复通过，损坏原文未改，写失败不虚报 | storage.test.js；round3、round4、final | 14、15、16、21 | 已验证 |
| 附加 | 修改标题后取消编辑 | 原信息保持 | 保持原标题 | round2 | 06（动作见日志） | 已验证 |
| 附加 | 删除确认中点击取消 | 保留该任务 | 数量仍1 | round2 | 08（动作见日志） | 已验证 |
| 附加 | 拖入空进行中/完成列 | 空列接受拖放 | 两次空列投放成功 | round4 | 11、12为主拖动证据 | 已验证 |
| 附加 | 删除最后一项再刷新 | 空状态，不重建演示数据 | localStorage 为 []，卡片数0 | round3 | 17为空态外观 | 已验证 |
| 附加 | 损坏 JSON、错误结构、读取拒绝 | 不白屏，原文不覆盖 | 提示明确，写入被阻止 | storage.test.js；round3、final | 21 | 已验证 |
| 附加 | 模拟写入失败和主题写入失败 | 不显示成功，不改变原数据/主题 | 表单保留，任务0，主题未切换 | storage.test.js；round3、final | 21为异常提示类证据，具体见断言 | 已验证 |
| 附加 | 超长标题、描述和含 script 字样文本 | 布局不破、完整详情、安全文本 | 390px无页面溢出，DOM完整含文本 | final | 19、20 | 已验证 |
| 附加 | 窄屏看板横向查看完成列 | 功能可达 | 完成列可滚入视口，页面不溢出 | final | 19为窄屏列表 | 已验证 |
| 附加 | npm run build | 构建成功 | 18 模块，exit 0 | build.txt | 不适用 | 已验证 |
| 附加 | 浏览器错误监测 | 无 pageerror / console.error | 空错误数组 | browser-console.json | 不适用 | 已验证 |
| 远程 | GitHub 创建、推送及 URL 核验 | 真实可访问仓库及正确 SHA | 无目标/授权，尚未推送 | github-delivery.md | 不适用 | 待完成 |

## 失败及修复记录

分轮失败见 development-log.md。最终第一次全量回归 7/8 通过，唯一失败为控制台 favicon 404，保留原始结果 `test-results/e2e-first-run-failed.txt`。添加真实 favicon 后重新全量运行，8/8 通过并重新生成所有截图。没有隐藏失败或用手动篡改存储冒充拖放。

## 覆盖边界

未配置 lint / TypeScript，因此这些检查未执行。没有进行所有浏览器兼容性、跨浏览器同步或多标签冲突测试。截图只证明可见状态；动作真实性由浏览器脚本和执行结果共同证明。没有部署需求，未部署。远程仓库未完成前不宣称全部交付完成。
`n补充：锁文件 npm ci 成功（install.txt）；实际生产预览在 Chrome 153.0.8010.52 完成创建/刷新/无控制台错误检查（production-smoke.json）。
